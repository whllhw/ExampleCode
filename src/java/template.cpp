#include <iostream>
#include <algorithm>

using namespace std;

void solve()
{
}

int main()
{
#ifndef ONLINE_JUDGE
    freopen("poj-3069.in", "r", stdin);
    freopen("stdout.txt", "w", stdout);
#endif

#ifndef ONLINE_JUDGE
    fclose(stdin);
    fclose(stdout);
#endif
    return 0;
}